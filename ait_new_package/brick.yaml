name: ait_new_package
description: A new brick created with the Mason CLI.

version: 0.1.0+2

environment:
  mason: ">=0.1.0-dev.51 <0.1.0"

vars:
  name:
    type: string
    description: The name of your Package
    default: new_package
    prompt: What is your package name? (must be snakeCase)
